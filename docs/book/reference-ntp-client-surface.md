# Reference: NTP Client Surface

Read this page when the path is generic NTP client posture rather than an
explicit probe or clock-discipline action.

Canonical entries covered here:

- `client`

Operational split:

- `client` is the default family landing point
- use the time subpage once the path is clearly a probe or sync workflow

Return to the family hub:

- [docs/book/reference-ntp-surface.md](docs/book/reference-ntp-surface.md)
