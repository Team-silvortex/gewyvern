# Reference: CoAP GET Surface

Read this page when the canonical entry is `get`.

Canonical entry:

- `get`

This slice is the default CoAP lookup path:

- send a lightweight resource request
- receive a content-oriented response

The `get` entry currently stays canonical without extra entry aliases.

Return to the family hub:

- [docs/book/reference-coap-surface.md](/Users/Shared/chroot/dev/gewyvern/docs/book/reference-coap-surface.md)
