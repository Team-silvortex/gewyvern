# Reference: DHCP Client Surface

Read this page when the canonical entry is `client`.

Canonical entry:

- `client`

This slice is the broad client-side posture page:

- a host behaves like a DHCP client
- the runtime is not yet narrowed to one exact lease-exchange step

The `client` entry currently stays canonical without extra entry aliases.

Return to the family hub:

- [docs/book/reference-dhcp-surface.md](docs/book/reference-dhcp-surface.md)
