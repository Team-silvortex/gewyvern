# Reference: SNMP Bulk Surface

Read this page when the path is table-oriented SNMP retrieval rather than
single-object lookup or mutation.

Canonical entry covered here:

- `bulk`

Current accepted aliases:

- `bulk-walk`
- `table-read`

Operational split:

- `bulk` requests wider traversal windows with one acknowledged response

Protocol package aliases also remain accepted:

- `snmp-bulk`
- `snmp_bulk`

Return to the family hub:

- [docs/book/reference-snmp-surface.md](/Users/Shared/chroot/dev/gewyvern/docs/book/reference-snmp-surface.md)
