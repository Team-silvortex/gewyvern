# Reference: TLS Client Surface

Read this page when the canonical entry is `client`.

Canonical entry:

- `client`

Aliases:

- `initiator`
- `tls-client`
- `tls_client`

This slice is the broad client-side TLS posture page:

- a local process initiates TLS toward a remote peer
- the runtime is not yet narrowed to one higher-level application overlay

Use these aliases when the operator thinks in terms of role ownership rather
than the exact canonical entry spelling:

- `initiator`
- `tls-client`
- `tls_client`

Use this slice before jumping into companion overlays such as:

- `https connect`
- `dns tcp`

Return to the family hub:

- [docs/book/reference-tls-surface.md](/Users/Shared/chroot/dev/gewyvern/docs/book/reference-tls-surface.md)

<!-- gewyvern:entry-aliases:start -->
## Current Entry Aliases

This generated block tracks the aliases that currently resolve into this custom surface.

- `initiator`
- `tls-client`
- `tls_client`

<!-- gewyvern:entry-aliases:end -->
