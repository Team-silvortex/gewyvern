[package]
name = "leserpent-cli"
version.workspace = true
edition.workspace = true
description = "Native authenticated CLI for the Leserpent control runtime"
license.workspace = true

[dependencies]
leselang-command = { path = "../leselang-command" }
leselang-syntax = { path = "../leselang-syntax" }
leserpent-domain = { path = "../leserpent-domain" }
leserpent-protocol = { path = "../leserpent-protocol" }
serde.workspace = true
serde_json.workspace = true
rustls.workspace = true
zeroize = "1"

[dev-dependencies]
leselang-hir = { path = "../leselang-hir" }
leserpent-adapters = { path = "../leserpent-adapters" }
leserpent-runtime = { path = "../leserpent-runtime" }
leserpentd = { path = "../leserpentd" }
rcgen = "0.14"

[[bin]]
name = "leserpent"
path = "src/main.rs"
