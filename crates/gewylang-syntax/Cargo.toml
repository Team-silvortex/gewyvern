[package]
name = "gewylang-syntax"
version.workspace = true
edition.workspace = true
description = "Product-independent GewyLang source, package, syntax tree, and frontend model"
license.workspace = true

[dependencies]
gewylang-contract = { path = "../gewylang-contract" }

[target.'cfg(unix)'.dependencies]
libc = "0.2"
