[package]
name = "leselang-command"
version.workspace = true
edition.workspace = true
description = "Pure lowering from authorized Leselang effects into Leserpent command plans"
license.workspace = true

[dependencies]
leselang-hir = { path = "../leselang-hir" }
leserpent-domain = { path = "../leserpent-domain" }
serde_json.workspace = true

[dev-dependencies]
leselang-syntax = { path = "../leselang-syntax" }
